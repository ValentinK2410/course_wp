# Быстрое исправление: каталог курсов теперь /courses/

## ✅ Что изменено

Slug архива курсов изменен с `/course/` на `/courses/` в файле:
- `course-plugin/includes/class-course-post-type.php`

## 🚀 Что нужно сделать СЕЙЧАС

### Шаг 1: Сбросить правила перезаписи URL

**Способ 1: Через админ-панель (самый простой)**
1. Войдите в админ-панель WordPress
2. Перейдите: **Настройки → Постоянные ссылки**
3. Нажмите: **Сохранить изменения** (ничего менять не нужно!)
4. Готово! ✅

**Способ 2: Через временный файл**
Создайте файл `reset-rewrite.php` в корне WordPress:

```php
<?php
require_once('wp-load.php');
flush_rewrite_rules();
echo "✅ Правила перезаписи URL сброшены!<br>";
echo "Каталог курсов теперь доступен по адресу: <a href='/courses/'>/courses/</a>";
```

Откройте в браузере: `https://mbs.russianseminary.org/reset-rewrite.php`
После проверки удалите файл.

### Шаг 2: Проверить работу

1. Откройте: `https://mbs.russianseminary.org/courses/`
2. Должны отображаться все курсы ✅

## 📝 Важно знать

- ✅ **Новый URL:** `/courses/` - работает
- ❌ **Старый URL:** `/course/` - больше не работает (после сброса rewrite rules)
- ✅ **Все ссылки в шаблоне** автоматически обновятся (используют `get_post_type_archive_link('course')`)
- ✅ **Код поддерживает оба варианта** для обратной совместимости

## 🔄 Если нужно вернуть обратно

Если нужно вернуть `/course/`:

1. Измените в `class-course-post-type.php`:
   ```php
   'rewrite' => array('slug' => 'course'),
   ```

2. Сбросьте rewrite rules (см. выше)

## 📞 Проблемы?

Если после изменений каталог курсов не открывается:

1. Убедитесь, что сбросили rewrite rules
2. Очистите кэш (если используется)
3. Проверьте права доступа к файлам

---

**Готово!** Каталог курсов теперь доступен по адресу `/courses/` 🎉
